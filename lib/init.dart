import 'package:flutter/material.dart';
import 'authenticate/sign_in.dart';

class AppInit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    
        // return MyAppPage(title: 'Mobile Number Auth',);
        return SignIn();
  }
}